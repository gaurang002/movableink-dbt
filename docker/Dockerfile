FROM postgres:15

# Optional: copy initialization scripts
COPY ./init.sql /docker-entrypoint-initdb.d/init.sql

# Optional: custom postgres.conf
# COPY ./postgres.conf /etc/postgresql/postgresql.conf
